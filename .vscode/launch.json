{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA GDB Launch",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/build/visualisierung"
        }
    ],
    "filterStdout": true,
    "filterStderr": true
}
